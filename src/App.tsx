import { FormEvent, useEffect, useMemo, useState } from 'react'
import { Navigate, Route, Routes, useLocation, useNavigate, useParams } from 'react-router-dom'
import { Activity, Bell, BookOpen, Building2, CalendarDays, CheckCircle2, ClipboardCheck, FileText, HeartHandshake, LockKeyhole, LogOut, MessageCircle, School, ShieldCheck, Stethoscope, UserRound, Users } from 'lucide-react'
import { supabase } from './supabase'
import { useAuth } from './auth'
import type { Portal } from './types'
import ParentPortal from './ParentPortal'

const portalCopy:Record<Portal,{title:string;subtitle:string;icon:typeof Users}>={parent:{title:'Parent Portal',subtitle:'Stay connected to your child’s learning, care and support.',icon:HeartHandshake},staff:{title:'Staff Portal',subtitle:'For teachers, special educators and therapists.',icon:Stethoscope},admin:{title:'Institute Admin',subtitle:'Secure institute access for authorized administrators.',icon:ShieldCheck}}
const roleLabel=(r:string)=>r.replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase())
function Shell({children}:{children:React.ReactNode}){return <main className="app-shell"><div className="ambient ambient-one"/><div className="ambient ambient-two"/>{children}</main>}
function Brand(){return <div className="brand"><div className="brand-mark">A</div><div><div className="brand-name">ANTAR</div><div className="brand-tagline">Every child leaves a mark.</div></div></div>}
function Landing(){const n=useNavigate();return <Shell><section className="auth-layout"><div className="intro-panel"><Brand/><div className="intro-copy"><span className="eyebrow">Secure care communication</span><h1>One calm place for families, care teams and institutions.</h1><p>ANTAR brings learning, care updates and communication together in one private institution-connected space.</p><div className="trust-row"><span><LockKeyhole size={17}/> Private by design</span><span><Building2 size={17}/> Institution connected</span></div></div></div><div className="portal-panel"><div className="portal-card"><div className="panel-heading"><span className="eyebrow">Choose your portal</span><h2>Welcome to ANTAR</h2><p>Select the account type assigned by your institution.</p></div><div className="portal-grid">{(Object.keys(portalCopy) as Portal[]).map(p=>{const x=portalCopy[p],I=x.icon;return <button className="portal-option" key={p} onClick={()=>n(`/login/${p}`)}><span className="portal-icon"><I size={22}/></span><span><strong>{x.title}</strong><small>{x.subtitle}</small></span><span className="arrow">→</span></button>})}</div><p className="legal-copy">Access is limited to accounts authorized by a participating institution.</p></div></div></section></Shell>}
function Login(){const {portal='parent'}=useParams();const p=(['parent','staff','admin'].includes(portal)?portal:'parent') as Portal;const copy=portalCopy[p],I=copy.icon;const {signIn,user,membership}=useAuth();const n=useNavigate();const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(user&&membership)return <Navigate to="/app" replace/>;async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await signIn(email.trim(),password,p);n('/app',{replace:true})}catch(x){setError(x instanceof Error?x.message:'Unable to sign in.')}finally{setBusy(false)}}return <Shell><section className="single-card-wrap"><div className="login-card"><button className="back-link" onClick={()=>n('/')}>← All portals</button><Brand/><div className="login-title-row"><span className="portal-icon large"><I size={24}/></span><div><span className="eyebrow">{copy.title}</span><h1>Sign in to ANTAR</h1></div></div><p className="login-subtitle">{copy.subtitle}</p><form onSubmit={submit} className="login-form"><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="text-button" type="button" onClick={async()=>{if(!email.trim())return setError('Enter your email address first.');const {error}=await supabase.auth.resetPasswordForEmail(email.trim(),{redirectTo:`${window.location.origin}${import.meta.env.BASE_URL}reset-password`});setError(error?error.message:'Password reset email sent.')}}>Forgot password?</button>{error&&<div className="status-message">{error}</div>}<button className="primary-button" disabled={busy}>{busy?'Signing in…':'Sign in securely'}</button></form></div></section></Shell>}
function ResetPassword(){const [password,setPassword]=useState(''),[status,setStatus]=useState('');const n=useNavigate();return <Shell><section className="single-card-wrap"><div className="login-card"><Brand/><h1>Choose a new password</h1><form className="login-form" onSubmit={async e=>{e.preventDefault();const {error}=await supabase.auth.updateUser({password});setStatus(error?error.message:'Password updated.');if(!error)setTimeout(()=>n('/'),800)}}><label>New password<input minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{status&&<div className="status-message">{status}</div>}<button className="primary-button">Update password</button></form></div></section></Shell>}

type Member={id:string;user_id:string;role:string;status:string;profiles?:{full_name?:string;phone?:string}|null}
type Child={id:string;first_name:string;last_name?:string;grade_or_program?:string;section?:string;student_identifier?:string;support_profile?:string;active:boolean}
type PLink={id:string;parent_id:string;child_id:string;relationship?:string;status:string}
type SLink={id:string;staff_id:string;child_id:string;assignment_type:string;active:boolean}
function AdminWorkspace({institutionId}:{institutionId:string}){const [tab,setTab]=useState<'people'|'children'|'assignments'>('people');const [members,setMembers]=useState<Member[]>([]),[children,setChildren]=useState<Child[]>([]),[plinks,setPLinks]=useState<PLink[]>([]),[slinks,setSLinks]=useState<SLink[]>([]);const [msg,setMsg]=useState(''),[loading,setLoading]=useState(true);const [childName,setChildName]=useState(''),[program,setProgram]=useState('');async function refresh(){setLoading(true);setMsg('');const [m,c,p,s]=await Promise.all([supabase.from('institution_members').select('id,user_id,role,status,profiles:user_id(full_name,phone)').eq('institution_id',institutionId).order('created_at'),supabase.from('children').select('id,first_name,last_name,grade_or_program,section,student_identifier,active').eq('institution_id',institutionId).order('first_name'),supabase.from('parent_child_links').select('*'),supabase.from('staff_child_assignments').select('*')]);if(m.error||c.error||p.error||s.error)setMsg(m.error?.message||c.error?.message||p.error?.message||s.error?.message||'Unable to load');else{setMembers((m.data||[]) as unknown as Member[]);setChildren((c.data||[]) as Child[]);setPLinks((p.data||[]) as PLink[]);setSLinks((s.data||[]) as SLink[])}setLoading(false)}useEffect(()=>{void refresh()},[institutionId]);async function patchMember(id:string,patch:Record<string,string>){const {error}=await supabase.from('institution_members').update(patch).eq('id',id);setMsg(error?error.message:'Access updated.');if(!error)await refresh()}async function addChild(e:FormEvent){e.preventDefault();const parts=childName.trim().split(/\s+/);const {error}=await supabase.from('children').insert({institution_id:institutionId,first_name:parts[0],last_name:parts.slice(1).join(' ')||null,grade_or_program:program||null,active:true});setMsg(error?error.message:'Child added.');if(!error){setChildName('');setProgram('');await refresh()}}async function parentLink(parent_id:string,child_id:string){const {error}=await supabase.from('parent_child_links').insert({parent_id,child_id,status:'active'});setMsg(error?error.message:'Parent linked.');if(!error)await refresh()}async function staffLink(staff_id:string,child_id:string,assignment_type:string){const {error}=await supabase.from('staff_child_assignments').insert({staff_id,child_id,assignment_type,active:true});setMsg(error?error.message:'Staff assigned.');if(!error)await refresh()}const parents=members.filter(m=>m.role==='parent'&&m.status==='active'),staff=members.filter(m=>['teacher','special_educator','therapist'].includes(m.role)&&m.status==='active');return <div><div className="dashboard-hero"><span className="eyebrow">Institute Admin</span><h1>People, children & access.</h1><p>Roles and child relationships are controlled by your institution and enforced by Supabase security policies.</p></div><div className="admin-tabs"><button onClick={()=>setTab('people')} className={tab==='people'?'active':''}>People & Access</button><button onClick={()=>setTab('children')} className={tab==='children'?'active':''}>Children</button><button onClick={()=>setTab('assignments')} className={tab==='assignments'?'active':''}>Assignments</button></div>{msg&&<div className="status-message admin-status">{msg}</div>}{loading?<div className="panel">Loading institution data…</div>:tab==='people'?<div className="panel"><div className="panel-title"><div><h2>People & Access</h2><p>Only an institute admin can change these roles and statuses.</p></div></div><div className="table-wrap"><table><thead><tr><th>Person</th><th>Role</th><th>Status</th><th>Access</th></tr></thead><tbody>{members.map(m=><tr key={m.id}><td><strong>{m.profiles?.full_name||'ANTAR member'}</strong><small>{m.profiles?.phone||'No phone added'}</small></td><td><select value={m.role} onChange={e=>void patchMember(m.id,{role:e.target.value})}>{['parent','teacher','special_educator','therapist','institute_admin'].map(r=><option key={r} value={r}>{roleLabel(r)}</option>)}</select></td><td><span className={`badge ${m.status}`}>{roleLabel(m.status)}</span></td><td><button className="mini-button" onClick={()=>void patchMember(m.id,{status:m.status==='active'?'suspended':'active'})}>{m.status==='active'?'Suspend':'Activate'}</button></td></tr>)}</tbody></table></div><p className="helper">New login accounts are created through secure Supabase Auth administration; ANTAR never places an administrator secret in the browser.</p></div>:tab==='children'?<div className="panel"><div className="panel-title"><div><h2>Child Management</h2><p>Add and manage children in this institution.</p></div></div><form className="inline-form" onSubmit={addChild}><input placeholder="Child name" value={childName} onChange={e=>setChildName(e.target.value)} required/><input placeholder="Class / programme" value={program} onChange={e=>setProgram(e.target.value)}/><button className="primary-button">Add child</button></form><div className="cards-list">{children.map(c=><div className="person-row" key={c.id}><div><strong>{c.first_name} {c.last_name}</strong><small>{c.grade_or_program||'Programme not added'}{c.section?` · Section ${c.section}`:''}{c.student_identifier?` · ${c.student_identifier}`:''}</small></div><span className={`badge ${c.active?'active':'suspended'}`}>{c.active?'Active':'Inactive'}</span></div>)}</div></div>:<div className="panel"><div className="panel-title"><div><h2>Parent & Staff Assignments</h2><p>These links decide exactly which children a parent or staff member can access.</p></div></div><h3>Link parent to child</h3><div className="assignment-grid">{parents.map(p=><AssignmentRow key={p.user_id} member={p} children={children} onAssign={id=>parentLink(p.user_id,id)}/>)}</div><h3>Assign staff to child</h3><div className="assignment-grid">{staff.map(s=><AssignmentRow key={s.user_id} member={s} children={children} onAssign={id=>staffLink(s.user_id,id,s.role)}/>)}</div><h3>Active relationships</h3><div className="cards-list">{plinks.filter(x=>x.status==='active').map(x=><Relation key={x.id} text={`${members.find(m=>m.user_id===x.parent_id)?.profiles?.full_name||'Parent'} → ${children.find(c=>c.id===x.child_id)?.first_name||'Child'}`} onOff={async()=>{await supabase.from('parent_child_links').update({status:'inactive'}).eq('id',x.id);await refresh()}}/>)}{slinks.filter(x=>x.active).map(x=><Relation key={x.id} text={`${members.find(m=>m.user_id===x.staff_id)?.profiles?.full_name||'Staff'} (${roleLabel(x.assignment_type)}) → ${children.find(c=>c.id===x.child_id)?.first_name||'Child'}`} onOff={async()=>{await supabase.from('staff_child_assignments').update({active:false}).eq('id',x.id);await refresh()}}/>)}</div></div>}</div>}
function AssignmentRow({member,children,onAssign}:{member:Member;children:Child[];onAssign:(id:string)=>void}){const [id,setId]=useState('');return <div className="assign-row"><div><strong>{member.profiles?.full_name||roleLabel(member.role)}</strong><small>{roleLabel(member.role)}</small></div><select value={id} onChange={e=>setId(e.target.value)}><option value="">Select child…</option>{children.filter(c=>c.active).map(c=><option key={c.id} value={c.id}>{c.first_name} {c.last_name}</option>)}</select><button className="mini-button" disabled={!id} onClick={()=>{onAssign(id);setId('')}}>Assign</button></div>}
function Relation({text,onOff}:{text:string;onOff:()=>void}){return <div className="person-row"><strong>{text}</strong><button className="mini-button danger" onClick={onOff}>Deactivate</button></div>}

type StaffClass={id:string;name:string;section?:string;academic_year?:string;count:number}
type StaffChild=Child & {todayStatus?:string}
function StaffPortal({userId}:{userId:string}){const [tab,setTab]=useState<'home'|'classes'|'children'|'attendance'>('home');const [classes,setClasses]=useState<StaffClass[]>([]);const [children,setChildren]=useState<StaffChild[]>([]);const [selectedClass,setSelectedClass]=useState('');const [attendanceDate,setAttendanceDate]=useState(new Date().toISOString().slice(0,10));const [attendance,setAttendance]=useState<Record<string,string>>({});const [loading,setLoading]=useState(true);const [message,setMessage]=useState('');async function load(){setLoading(true);setMessage('');const classRows=await supabase.from('class_staff_assignments').select('class_id,classes(id,name,section,academic_year),active').eq('staff_id',userId).eq('active',true);if(classRows.error){setMessage(classRows.error.message);setLoading(false);return}const classList=(classRows.data||[]).map((row:any)=>({id:row.classes.id,name:row.classes.name,section:row.classes.section,academic_year:row.classes.academic_year,count:0}));const childRows=await supabase.from('staff_child_assignments').select('child_id,children(id,first_name,last_name,grade_or_program,section,student_identifier,support_profile,active)').eq('staff_id',userId).eq('active',true);if(childRows.error){setMessage(childRows.error.message);setLoading(false);return}const assigned=((childRows.data||[]).map((r:any)=>r.children).filter(Boolean)) as StaffChild[];for(const c of classList){const enroll=await supabase.from('class_enrollments').select('child_id',{count:'exact',head:true}).eq('class_id',c.id).eq('active',true);c.count=enroll.count||0}setClasses(classList);setChildren(assigned);if(!selectedClass&&classList[0])setSelectedClass(classList[0].id);setLoading(false)}useEffect(()=>{void load()},[userId]);const currentClass=classes.find(c=>c.id===selectedClass);const classChildren=useMemo(()=>children.filter(c=>!currentClass||c.grade_or_program===currentClass.name&&(currentClass.section?c.section===currentClass.section:true)),[children,currentClass]);async function loadAttendance(){if(!selectedClass)return;const ids=classChildren.map(c=>c.id);if(!ids.length){setAttendance({});return}const {data,error}=await supabase.from('attendance').select('child_id,status').eq('attendance_date',attendanceDate).in('child_id',ids);if(error){setMessage(error.message);return}const next:Record<string,string>={};ids.forEach(id=>next[id]='present');(data||[]).forEach((r:any)=>next[r.child_id]=r.status);setAttendance(next)}useEffect(()=>{void loadAttendance()},[selectedClass,attendanceDate,children.length]);async function saveAttendance(){if(!classChildren.length)return;setMessage('Saving attendance…');const payload=classChildren.map(c=>({child_id:c.id,attendance_date:attendanceDate,status:attendance[c.id]||'present',recorded_by:userId}));const {error}=await supabase.from('attendance').upsert(payload,{onConflict:'child_id,attendance_date'});if(error){setMessage(error.message);return}await loadAttendance();setMessage('Attendance saved successfully.')}function markAll(status:string){const next:Record<string,string>={};classChildren.forEach(c=>next[c.id]=status);setAttendance(next)}return <div className="staff-portal"><div className="staff-nav"><button className={tab==='home'?'active':''} onClick={()=>setTab('home')}><School size={17}/>Home</button><button className={tab==='classes'?'active':''} onClick={()=>setTab('classes')}><BookOpen size={17}/>Assigned Classes</button><button className={tab==='children'?'active':''} onClick={()=>setTab('children')}><Users size={17}/>Assigned Children</button><button className={tab==='attendance'?'active':''} onClick={()=>setTab('attendance')}><ClipboardCheck size={17}/>Attendance</button></div>{message&&<div className="status-message admin-status">{message}</div>}{loading?<div className="panel">Loading staff workspace…</div>:tab==='home'?<><div className="dashboard-hero"><span className="eyebrow">Staff Portal</span><h1>Your classes and children, in one place.</h1><p>Everything below is loaded from your authenticated Supabase assignments.</p></div><div className="feature-grid"><button className="feature-card" onClick={()=>setTab('classes')}><span className="feature-icon"><BookOpen size={21}/></span><span><strong>{classes.length} assigned classes</strong><small>Open classes and class rosters</small></span><span className="arrow">→</span></button><button className="feature-card" onClick={()=>setTab('children')}><span className="feature-icon"><Users size={21}/></span><span><strong>{children.length} assigned children</strong><small>View authorized child profiles</small></span><span className="arrow">→</span></button><button className="feature-card" onClick={()=>setTab('attendance')}><span className="feature-icon"><ClipboardCheck size={21}/></span><span><strong>Attendance</strong><small>Mark and save class attendance</small></span><span className="arrow">→</span></button></div></>:tab==='classes'?<div className="panel"><div className="panel-title"><div><h2>Assigned Classes</h2><p>Classes are derived from class_staff_assignments.</p></div></div><div className="feature-grid">{classes.map(c=><button key={c.id} className="feature-card" onClick={()=>{setSelectedClass(c.id);setTab('attendance')}}><span className="feature-icon"><School size={21}/></span><span><strong>{c.name}{c.section?` — ${c.section}`:''}</strong><small>{c.count} children · {c.academic_year||'Academic year not set'}</small></span><span className="arrow">→</span></button>)}</div></div>:tab==='children'?<div className="panel"><div className="panel-title"><div><h2>Assigned Children</h2><p>{children.length} children available through your staff assignments.</p></div></div><div className="cards-list">{children.map(c=><div className="person-row" key={c.id}><div><strong>{c.first_name} {c.last_name}</strong><small>{c.grade_or_program||'Class not set'}{c.section?` · Section ${c.section}`:''}</small></div><span className="badge active">Assigned</span></div>)}</div></div>:<div className="panel"><div className="panel-title"><div><h2>Attendance</h2><p>Select a class and date, then save the real Supabase attendance records.</p></div></div><div className="attendance-toolbar"><label>Class<select value={selectedClass} onChange={e=>setSelectedClass(e.target.value)}>{classes.map(c=><option key={c.id} value={c.id}>{c.name}{c.section?` — ${c.section}`:''}</option>)}</select></label><label>Date<input type="date" value={attendanceDate} onChange={e=>setAttendanceDate(e.target.value)}/></label><button className="mini-button" onClick={()=>markAll('present')}>Mark all present</button></div><div className="cards-list">{classChildren.map(c=>{const status=attendance[c.id]||'present';return <div className="attendance-row" key={c.id}><div><strong>{c.first_name} {c.last_name}</strong><small>{c.grade_or_program}{c.section?` · Section ${c.section}`:''}</small></div><div className="attendance-actions">{['present','absent','late','excused'].map(s=><button key={s} className={`attendance-pill ${status===s?'selected':''}`} onClick={()=>setAttendance(prev=>({...prev,[c.id]:s}))}>{roleLabel(s)}</button>)}</div></div>})}</div><div className="attendance-save"><button className="primary-button" onClick={()=>void saveAttendance()}><CheckCircle2 size={17}/> Save Attendance</button></div></div>}</div>}

function ProtectedApp(){const {user,membership,loading,signOut}=useAuth();const loc=useLocation();if(loading)return <Shell><div className="loading-screen">Loading ANTAR…</div></Shell>;if(!user||!membership)return <Navigate to="/" replace state={{from:loc}}/>;const institutionName=membership.institutions?.name??'Your institution',role=membership.role;return <Shell><section className="dashboard-shell"><header className="dashboard-header"><Brand/><div className="header-actions"><div className="account-chip"><small>{institutionName}</small><strong>{roleLabel(role)}</strong></div><button className="secondary-button" onClick={()=>void signOut()}><LogOut size={17}/> Logout</button></div></header><div className="dashboard-content">{role==='institute_admin'?<AdminWorkspace institutionId={membership.institution_id}/>:role==='parent'?<ParentPortal userId={user.id}/>:<StaffPortal userId={user.id}/>}<div className="identity-strip"><ShieldCheck size={18}/><span>Protected session</span><span>{user.email}</span><span>{institutionName}</span></div></div></section></Shell>}
export default function App(){return <Routes><Route path="/" element={<Landing/>}/><Route path="/login/:portal" element={<Login/>}/><Route path="/reset-password" element={<ResetPassword/>}/><Route path="/app" element={<ProtectedApp/>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes>}
